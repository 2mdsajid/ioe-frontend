import { STREAM } from "./base.schema";

export type TPremiumPricingData = {
            actualPrice: number;
            discountPrice: number;
            description: string;
            duration: string;
        }